/data/
cache/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
*.egg-info/
dist/
build/
.DS_Store

# Local-only operator runbook (kept on disk, not tracked)
RUNPOD.md

# Derived analysis bundle (per-frame embeddings + indices, ~150-250MB).
# Re-exported on demand by scripts/export_eval_bundle.py; never tracked.
# Diagnostic OUTPUTS under results/tables/ and results/plots/ ARE tracked.
results/bundle/
