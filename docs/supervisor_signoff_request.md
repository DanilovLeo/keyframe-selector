# Запрос на согласование (черновик письма — НЕ отправлено)

**Тема:** Согласование методологических дополнений — Variant C (keyframe selection)

Здравствуйте, [Имя Отчество]!

Кратко фиксирую несколько дополнений к утверждённому плану и прошу подтвердить,
что они остаются в согласованных рамках (pixels-only, intrinsic retrieval, без
policy training, без rollouts, без robot-state и без новых датасетов). Пункты 1–3
уже реализованы; пункт 4 — на будущее и не будет запускаться до Вашего ответа.

1. **frame_diff** как пятый метод (в дополнение к трём из брифа + random).
   Это baseline по вычислительной стоимости — чистый NumPy, без обучаемого
   backbone; на нашей метрике он статистически не отличим от RAFT и DINOv2, что
   усиливает вывод: предобученные сети не дают выигрыша на этом бенчмарке.

2. **DINOv2 ViT-S/14** как конкретная реализация упомянутых в брифе
   «ViT attention maps». Self-supervised ViT attention — стандартный выбор для
   saliency; attention-карты supervised/CLIP-ViT заметно более шумные.

3. **Coverage error** как принятая четвёртая intrinsic-метрика (отчёт всегда
   рядом с тремя основными; см. запись в `docs/decisions.md` от 2026-06-10). Это
   расстояние до ближайшего выбранного кадра в замороженном embedding-пространстве
   — не reconstruction и не обучение.

4. (опционально, на будущее — **только после Вашего согласия**):
   - **Residual embeddings** — разности уже закэшированных замороженных
     embeddings (e_t − e_0 или e_t − mean), без модели и без обучения. Может быть
     запущен как gated-диагностика, но включение результата в отчёт ждёт
     согласования.
   - **Вторая камера `image_1`** — тот же датасет, тот же pipeline, одна строка
     конфигурации; напрямую проверяет механизм scene-dominance.

Пункты 1–3 прошу подтвердить, пункт 4 — обсудить. Спасибо!

С уважением,
[Имя]
